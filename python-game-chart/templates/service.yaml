apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-game.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "python-game.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 80
